# BARFI

This repository now hosts a Barfi photo gallery with an auto-playing slideshow, side controls, and cute captions.

## What it does

- Plays through Barfi's photos automatically
- Lets you move back and forward from the sidebar
- Shows a thumbnail strip for quick jumps

The published site is built from `index.html` and the photos in `assets/`.
